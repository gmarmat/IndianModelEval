# HuggingFace token — required for gated models and dataset downloads
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=

# Sarvam API key — only needed if using API mode (--use-api flag)
# Default is local weights. Leave blank for local inference.
SARVAM_API_KEY=
