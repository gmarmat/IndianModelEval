# This module is licensed under GPL-3.0.
# It is called via subprocess from eval_runner.py — never imported directly.
# The core harness (MIT) maintains license isolation through subprocess boundary.
